const PLANTS = {
  'C': 'clover',
  'G': 'grass',
  'R': 'radishes',
  'V': 'violets'
};

const STUDENTS = {
  alice,
  bob,
  charlie,
  david,
  eve,
  fred,
  ginny,
  harriet,
  ileana,
  joseph,
  kincaid,
  larry
}

class Garden {
  constructor(input) {
    this.rowsArray = input.replace(/\n/, '').split('')
    this.alice =
  }
}

if (module) { module.exports = Garden; }
