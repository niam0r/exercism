export class Matrix {
  constructor(strInput) {
    this.rows = this.buildMatrix(strInput);
    this.colums = this.transpose(this.rows);
    this.saddlePoints = this.findSaddlePoints();
  }

  buildMatrix(strInput) {

  }

  transpose(rows) {

  }

  findSaddlePoints() {

  }
}
